{
  "name": "email-service",
  "version": "1.0.0",
  "description": "A resilient email sending service with retry, fallback, idempotency, rate limiting, and status tracking.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "mocha test/EmailService.test.js --timeout 10000"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "^10.0.0",
    "chai": "^4.3.6"
  }
}
